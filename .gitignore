# Electric Sheep public repo — ONLY these files are public
# Everything else in this workspace is local-only. Add nothing else.
/*
!/index.html
!/CNAME
!/.gitignore
!/.nojekyll
!/.github
!/.github/**
